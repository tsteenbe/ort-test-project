- id: "NuGet:Microsoft.Extensions:Caching.SqlServer"
  curations:
    comment: |
      Enforce scanning the code repository because the source artifact does not contain all corresponding source files.
    source_code_origins: [VCS]

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[10.0.0,)"
  curations:
    comment: |
      Package code repository location is not declared, or the metadata is unavailable in internal Artifactory.
    vcs:
      type: "Git"
      url: "https://github.com/dotnet/dotnet.git"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[10.0.0,)"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/aspnetcore/src/Caching/SqlServer/src"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[6.0.0,10.0.0["
  curations:
    comment: |
      Package code repository location is not declared, or the metadata is unavailable in internal Artifactory.
    vcs:
      type: "Git"
      url: "https://github.com/dotnet/aspnetcore.git"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[6.0.0,10.0.0["
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Caching/SqlServer/src"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[2.1.2,6.0.0["
  curations:
    comment: |
      Package code repository location is not declared, or the metadata is unavailable in internal Artifactory.
    vcs:
      type: "Git"
      url: "https://github.com/dotnet/extensions.git"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[2.1.2,6.0.0["
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Caching/SqlServer/src"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[1.0.0,2.1.2["
  curations:
    comment: |
      Package code repository location is not declared, or the metadata is unavailable in internal Artifactory.
    vcs:
      type: "Git"
      url: "https://github.com/aspnet/Caching.git"

- id: "NuGet:Microsoft.Extensions:Caching.SqlServer:[1.0.0,2.1.2["
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Microsoft.Extensions.Caching.SqlServer"
