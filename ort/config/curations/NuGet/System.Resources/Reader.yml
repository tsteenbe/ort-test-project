- id: "NuGet:System.Resources:Reader:[5.0.0,)"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/libraries/System.Resources.Reader"
