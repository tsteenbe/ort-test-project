- id: "NuGet:Azure.Messaging:EventGrid.SystemEvents"
  curations:
    comment: |
      Enforce scanning the code repository because the source artifact does not contain all corresponding source files.
    source_code_origins: [VCS]

- id: "NuGet:Azure.Messaging:EventGrid.SystemEvents"
  curations:
    comment: |
      Package code repository location is not declared, or the metadata is unavailable from internal package registries (e.g., Artifactory).
    vcs:
      type: "Git"
      url: "https://github.com/Azure/azure-sdk-for-net.git"

- id: "NuGet:Azure.Messaging:EventGrid.SystemEvents"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "sdk/eventgrid/Azure.Messaging.EventGrid.SystemEvents/src"

- id: "NuGet:Azure.Messaging:EventGrid.SystemEvents:[1.0.0,)"
  curations:
    comment: |
      Map declared license based on
      https://github.com/Azure/azure-sdk-for-net/blob/v1.0.0/LICENSE.txt
    declared_license_mapping:
      "https://www.nuget.org/packages/Azure.Messaging.EventGrid.SystemEvents/1.0.0/license": "Apache-2.0"