- id: "NuGet:Azure.Containers:ContainerRegistry"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "sdk/containerregistry/Azure.Containers.ContainerRegistry"
