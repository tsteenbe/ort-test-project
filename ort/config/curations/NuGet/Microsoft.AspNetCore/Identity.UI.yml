- id: "NuGet:Microsoft.AspNetCore:Identity.UI:2.1.7"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Identity/src/UI"
- id: "NuGet:Microsoft.AspNetCore:Identity.UI:2.2.1"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Identity/src/UI"
- id: "NuGet:Microsoft.AspNetCore:Identity.UI:[2.1.8,2.1.31]"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Identity/UI/src"
- id: "NuGet:Microsoft.AspNetCore:Identity.UI:[2.2.2,)"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Identity/UI/src"
