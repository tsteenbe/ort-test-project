- id: "NuGet:Microsoft.AspNetCore:HttpLogging:[6.0.0,)"
  curations:
    comment: |
      Package resides in its own directory within code repository.
    vcs:
      path: "src/Middleware/HttpLogging/src"
